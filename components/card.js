export default class Card {
  constructor({ name, link }) {
    this._name = name;
    this._link = link;
    console.log(name);
  }

  getCard() {
    // get the card view
    // set event listeners
    //return card
  }
}
